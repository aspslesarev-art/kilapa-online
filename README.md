# kilapa.online (CCV landing)

Static landing page for CCV — a clipboard tool that lives in the MacBook notch.

## Local

```bash
npm install
npm start
# open http://localhost:3000
```

## Deploy

Auto-deployed on Railway from this repo's `main` branch.
Custom domain: kilapa.online
